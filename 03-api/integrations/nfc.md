# NFC/NDEF 集成契约

## 状态

待硬件和供应商确认。

## 必须确认

- 介质类型。
- UID 读取方式。
- NDEF 内容格式。
- 写卡设备和 SDK。
- 读卡设备和 SDK。
- 写入失败返回。
- 读回核验方式。
- 批量写卡能力。

## 后端边界

- 后端保存资产和 Payload 业务记录。
- 后端保存核验结果和外部同步状态。
- 物理写卡通过 `NfcAdapter` 抽象。
- 没有硬件时使用 Mock，不伪造真实核验成功。
