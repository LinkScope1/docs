# 幂等规范

## 适用对象

- 绑定、解绑、转交。
- 资产导入和状态命令。
- 路由发布。
- LinkForty API 写入。
- Webhook 接收。

## 规则

- 客户端命令使用 `Idempotency-Key`。
- Webhook 使用 `event_id` 全局唯一。
- `click_id` 不得作为唯一键。
- 数据库唯一约束是并发最终防线。
- 重复请求返回第一次请求的最终结果或幂等成功。
- 外部 API 请求必须携带稳定的幂等标识。

## 禁止

- 只用 Redis 做最终幂等判断。
- 只用内存集合保存幂等键。
- 用到达时间替代事件时间。
