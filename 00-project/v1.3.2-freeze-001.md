# V1.3.2-FREEZE-001 冻结记录

## 1. 冻结定位

| 项目 | 内容 |
| --- | --- |
| 冻结编号 | `V1.3.2-FREEZE-001` |
| 目标版本 | V1.3.2 |
| 冻结对象 | 物理模型、V3.1 模块方案、权限矩阵、API 规范、7 张表数据字典、状态机 |
| 当前状态 | `PARTIAL_EXTERNAL_EVIDENCE_PENDING` |
| 规则 | 技术默认值直接采用；外部事实必须有负责人、日期和证据链接后才可关闭 |
| 变更方式 | 任何冻结内容变化必须新增决策记录并更新版本/影响分析 |

本记录用于关闭 P0 冻结工作的本地决策和证据门禁，不代表银行后台业务功能已经实现。

- 本次工作包重组只影响研发组织和任务归属，不改变模块编号、数据所有权、Router → Service → Repository 分层、LinkForty API-only 边界、NFC Mock 边界、9 个阻塞任务、`PARTIAL_EXTERNAL_EVIDENCE_PENDING` 或 V1.3.2 不得生产发布的限制。

## 2. 已冻结的本地基线

- 银行后台只拥有 `bank_admin` Schema 的 7 张业务表。
- 正式业务模块固定为 M1～M5；旧 M6～M8 只允许出现在历史迁移说明中。
- Casdoor 是身份、角色和功能权限权威；银行后台不创建本地 `iam_*` 表。
- LinkForty 写入只能通过 API；LinkForty 表不得由银行迁移或业务 Repository 修改。
- `event_id` 是访问事件全局幂等键，`click_id` 只作非唯一逻辑引用。
- `access_events.asset_id` 非空；资产无法唯一解析时不得写入 `access_events`。
- Payload 不保存外部同步状态；外部调用结果由 M1 审计并通过 `trace_id` 关联。
- 绑定只追加历史，解绑结束当前记录；V1.3.2 只支持立即生效绑定。
- M4 绑定 Service 已落到既有 `/api/v1/touchpoint-assignments` 路径：资产先锁、当前绑定后锁；绑定/解绑/调拨在本地事务内同步资产、Payload 和 `operation_logs`，不新增表或通用幂等存储。
- 导入只提供同步预校验；异步执行、任务状态和结果持久化延期 V1.4。
- V1.3.2 同步导出默认限制为 10 MiB 或 100,000 行；异步导出延期 V1.4。
- 统计读取 M5 本地事件，并通过 LinkForty API-only 边界读取 Core 已提供的数据；不创建本地统计表，不连接 LinkForty 数据库。

## 2.1 V1.3.2 外部集成延期决策（2026-08-24）

- Casdoor 的 issuer、audience、JWKS、Claim、角色/权限、停用同步和真实环境外部联调任务（`P0-CAS-001~006`、`DEC-CAS-001~006`）延期至 V1.4；V1.3.2 不实现生产 Casdoor Adapter。
- NFC 的真实介质、UID/NDEF 格式、硬件/SDK、真机写卡和读回核验任务（`P0-NFC-001~002`、`DEC-NFC-001~002`）延期至 V1.4；V1.3.2 不纳入真实浏览器接入、硬件 SDK 或真机验收。
- V1.3.2 保留 `AccessContext`、`MockAccessContextProvider`、`NfcDevice` 和 `MockNfcDevice` 作为开发/测试基线；Mock 成功或失败结果不得替代真实外部证据。
- 未配置真实认证的生产环境必须拒绝访问；在 Casdoor/NFC 外部证据补齐前，V1.3.2 冻结状态继续为 `PARTIAL_EXTERNAL_EVIDENCE_PENDING`，不得作为生产版本发布。
- V1.4 跟踪 Issue： [Casdoor 真实认证与权限外部联调](https://github.com/LinkScope1/docs/issues/9)；[NFC 硬件与 SDK 写卡读回核验联调](https://github.com/LinkScope1/docs/issues/10)。

## 2.2 本次阻塞收敛验证（2026-08-24）

- 银行后端全量测试：`94 passed, 1 skipped`（通过 `DATABASE_URL` 指向本地隔离 PostgreSQL，并启用真实数据库集成测试）；数据库测试使用事务回滚，未留下测试组织。
- 银行后端 Ruff、mypy、前端 lint/typecheck/build、LinkForty Core 自有测试（`188 passed`）和文档校验（91 个 Markdown 文件）均通过。
- 本地 Docker 服务健康检查已通过：银行 PostgreSQL 15.19、银行 Redis 7.4.11、LinkForty Core `/health/ready` 以及 Core PostgreSQL/Redis 均可用；银行库为 `bank_admin`，Alembic 当前 revision 为 `0002_bank_v13_physical_model`，未在本次任务中运行迁移或修改数据库表。
- 银行 Redis 的 DB0/DB1/DB2 已按配置分离且当前为空，但银行后台 API/Worker 未在本地 Compose 中运行，队列隔离和生产配置证据仍待补。对本地银行测试库的角色检查只发现 `bank_admin`，独立迁移账号和独立运行账号仍待补；LinkForty 数据库只读账号、授权和负向测试明确延期至 V1.4，V1.3.2 统计不建立或使用该账号。
- LinkForty 本地真实 API 健康、创建/查询和点击分析 API 可用，但相同 `Idempotency-Key` 的重复创建请求产生了两个不同 Link；临时 Link 已删除。该行为作为 V1.3.2 已知缺陷接受，不标记为幂等通过，也不创建 V1.4 修复任务；正式错误字段、TLS/ACL 安全例外和 API 生产配置证据仍待补。
- 本地 Mock、MockTransport 和单元测试结果仅证明开发/测试边界，不替代 LinkForty、Casdoor、NFC 或生产运行环境证据。

## 2.3 API 契约与任务治理收敛（2026-08-24）

- `03-api/openapi.yaml` 是 V1.3.2 唯一机器源，41 个操作均有唯一 `operationId`、负责人、权限、数据范围、幂等、审计和契约状态；模块 YAML 通过 `x-canonical-path` 和 `operationId` 作为评审镜像。
- M2/M3/M4 的 12 个通用 `JsonObject` 请求体已替换为严格请求 Schema；组织编码和员工组织不能通过普通修改请求改变；统一 BIGINT 字符串、错误响应和权限元数据。
- `GET /api/v1/analytics/summary` 冻结四项汇总计数、时间范围、`orgCodePrefix` 范围收窄和数据源不可用 503 语义，不创建本地统计表或趋势数组；V1.3.2 的 LinkForty 数据只通过 API 获取。
- `scripts/validate_openapi_contract.py` 和 `scripts/validate_task_governance.py` 已纳入 docs CI；校验模块镜像、权限映射、Issue/Epic/任务追踪、阻塞字段完整性和 V1.4 延期边界。
- `P0-API-002`、`P0-GOV-002` 在契约和治理校验通过后标记为 `已确认`；`P0-API-007`、`X-ANL-001` 标记为 `条件开发`，因为 Core API 的安装/App 聚合能力和精确时间窗口仍未确认。
- 本节只关闭契约和追踪关系；LinkForty 数据库只读账号已明确延期至 V1.4，API-only 统计仍受 Core 能力和真实 API 配置证据约束；性能阈值、发布门禁和其他外部证据阻塞继续保持，冻结状态为 `PARTIAL_EXTERNAL_EVIDENCE_PENDING`。

## 2.4 剩余阻塞收敛实施（2026-08-24）

- M2 组织/员工 PATCH、启用/停用、员工调动已落到 `Router → Service → Repository`；`orgCode`/`orgId` 不进入普通修改请求，当前绑定会阻止员工停用和组织变更，所有命令写入 `operation_logs`。
- V1.3.2 同步导入预校验已落到 `/api/v1/imports/validate`：UTF-8 CSV、模板列、稳定键、逐行错误和预计新增/更新/跳过；不写业务表、不创建导入批次/明细表，异步执行继续延期 V1.4。
- 同步导出已落到 `/api/v1/exports/{resource}`，字段白名单、权限/范围过滤、10 MiB/100,000 行上限、413 和审计已固定；异步导出、对象存储和下载 Token 继续延期 V1.4。
- analytics 汇总已落到 `/api/v1/analytics/summary`；访问统计读取本地 `access_events.received_at`，LinkForty 数据只通过 API-only 适配器读取；Core 未提供的安装/App 聚合能力统一返回 `503 DATA_SOURCE_UNAVAILABLE`，不返回部分成功。
- LinkForty 数据库只读账号、SQL 授权脚本和负向测试脚本延期至 V1.4；银行后台 `database/checks/` 中的脚本仅为未来外部证据准备，不能作为 V1.3.2 运行依赖或验收证据。
- 性能/发布决策已冻结：列表/事件/统计/同步导出 P95 分别为 500ms/1s/2s/5s，错误率 <1%，队列积压 <100，观察 30 分钟；P99 仅观测。实际压测、生产配置、备份恢复、回滚和发布审批仍未完成。
- 真实 Casdoor、NFC 和 LinkForty 同 Key 重复创建 Link 的已知缺陷边界不变；V1.3.2 只能继续开发、测试和内部验收，冻结状态仍为 `PARTIAL_EXTERNAL_EVIDENCE_PENDING`，不得生产发布。
- 9 个延期收敛阻塞任务（`P1-ENV-004`、`X-ANL-004`、`X-WORK-003`、`T-PERF-001`、`R-P3-007`、`R-P5-001`、`R-P5-004`、`R-P5-008`、`R-P5-010`）继续保持 `阻塞待确认`；任务清单已补齐负责人、前置条件、证据文件、执行入口、关闭条件和版本限制。
- 当前只推进治理准备，不提前执行隔离数据库/Redis、性能压测、生产配置审批、发布冒烟或发布后观察。执行顺序为：隔离环境 → API-only 统计/Worker 对账能力确认 → 性能报告 → 生产安全审批 → 发布门禁与冒烟 → 正式批准后的 30 分钟观察。
- `X-ANL-004` 在 Core 未提供正式安装/App 聚合 API 时保持条件阻塞；不得查询 LinkForty 数据库、使用 `bank_linkforty_ro`、返回伪造零值或部分成功。LinkForty 数据库只读账号继续延期至 V1.4。
- 当前阶段固定结论：V1.3.2 可继续开发、测试和内部验收，但因 Casdoor/NFC 外部证据、隔离/生产环境证据、性能报告和发布门禁未完成，不得生产发布。

## 3. P0 关闭矩阵

| 决策域 | 结论 | 关闭证据 | 状态 |
| --- | --- | --- | --- |
| 系统边界 | 银行库、Casdoor、LinkForty、NFC 和演示项目职责固定 | 项目章程、架构评审记录 | 已具备本地基线 |
| V1.3.2/V3.1 | 7 张银行表、M1～M5、工作包 A（M1～M3）和工作包 B（M4～M5）固定 | 数据字典、MVP 模块方案 | 已具备本地基线 |
| Webhook | HMAC-SHA256、原始 body、字段路径和 `event_id` 契约已确认 | 脱敏样例、真实联调报告、负责人确认 | 已确认 |
| Casdoor | issuer、audience、JWKS、算法、Claim、角色和停用策略 | V1.4 外部联调证据；V1.3.2 保留 Mock 访问上下文 | V1.4 延期；外部证据待补 |
| LinkForty API | `/api/links` 创建/查询、银行侧 Adapter、稳定 Key 转发和有界重试 | Adapter 单测、MockTransport、请求键和重试报告；Core 同 Key 重复创建缺陷复现 | 本地适配器已验证；Core 幂等作为 V1.3.2 已知风险 |
| LinkForty 只读 | 数据库只读账号和 SQL 白名单延期；V1.3.2 使用 API-only 读取边界 | V1.4 DBA 权限脚本、INSERT/DDL/TRUNCATE 负向测试；V1.3.2 API 适配器测试 | V1.4 延期 |
| NFC | UID、NDEF、设备失败和读回核验契约固定 | V1.4 硬件/SDK 联调证据；V1.3.2 保留 Mock 失败测试 | V1.4 延期；外部证据待补 |
| 数据库/Redis | 迁移、运行账号、Snowflake 节点和队列隔离固定 | Alembic、权限记录、环境验证报告 | 本地基线具备；真实环境证据待补 |
| 权限/数据范围 | 权限码、角色范围和六类 Scope 固定 | V1.4 Casdoor 权限导出、矩阵和越权报告；V1.3.2 保留 Mock 上下文测试 | Casdoor 真实证据延期至 V1.4 |
| API | `/api/v1`、Envelope、错误码、BIGINT 字符串和分域路径固定 | OpenAPI YAML、模块镜像、权限映射、`validate_openapi_contract.py` | 契约已冻结；业务实现和外部字段证据仍按任务清单验收 |
| 数据模型/状态机 | 枚举、唯一、排他、追加写和状态转换固定 | 数据字典、迁移、模型测试、纯状态机合法/非法转换测试 | 本地契约已验证 |

## 3.1 P0 阻塞项逐项落点

| 清单项 | 本次解决方案落点 | 当前门禁 |
| --- | --- | --- |
| `P0-BASE-001/002/003` | 仅银行库 7 表；Casdoor、LinkForty、NFC 和演示项目边界固定；不建设本地 IAM、目标资源、路由规则和导入执行表 | 本地方案已冻结；架构负责人签字待补 |
| `P0-CAS-001~006`、`P0-SEC-001` | V1.3.2 保留 `AccessContext` 和 Mock 权限/数据范围测试边界；真实 Casdoor issuer、audience、JWKS、Claim、角色导出、停用和生产 Adapter 延期至 V1.4 | V1.4 外部证据待补；不得将 Mock 结果标记为真实联调通过 |
| `P0-LF-001/002` | 银行侧 Adapter 基于 `POST /api/links`、`GET /api/links/{id}`；写入稳定幂等键；4xx 不重试，429/5xx/超时最多 3 次退避 | Core 健康和 API 创建/查询已验证；同 Key 重复请求产生两个 Link，作为 V1.3.2 已知缺陷；TLS/ACL 和正式字段证据仍待外部确认 |
| `P0-LF-005` | LinkForty 数据库只读账号和白名单延期至 V1.4；V1.3.2 统计只调用 API，不读取数据库 | V1.4 DBA 权限脚本和负向测试；V1.3.2 API-only 适配器测试 |
| `P0-NFC-001/002` | V1.3.2 保留 NFC-only 接口、UID/NDEF/长度契约和写失败、读回失败、不一致的 Mock 边界；真实设备、SDK、写卡和读回核验延期至 V1.4 | V1.4 设备型号、编码、长度和供应商确认单待补 |
| `P0-DB-001/002`、`P0-REDIS-001` | PostgreSQL 14+、`bank_admin`、`0003` 新 revision、Snowflake 节点配置、API BIGINT 字符串、Redis 非事实存储 | PostgreSQL/Redis 健康已验证；当前 revision 为 `0002`，`0003`/迁移权限、节点唯一性、Worker 队列隔离和 Redis 故障报告待补 |
| `P0-API-001/002` | OpenAPI 版本 1.3.2；M4/M5 片段；每个接口统一五类扩展；移除 `/imports/execute`；同步导出 10 MiB/100,000 行 | YAML、引用、权限码、模块状态和路由越权测试已纳入本地 CI |
| `DEC-DATA-001/002` | `access_events.asset_id` 非空；LinkForty Link 非空 UUID 全局唯一；歧义事件拒写事件表并保留安全审计/补偿 | 冻结记录已确认；迁移实库、并发和歧义重放属于后续验证证据 |
| `DEC-IDEMP-001` | V1.3.2 采用 event_id、领域唯一键、状态命令和绑定区间；不新增通用幂等表；通用响应回放延期 V1.4 | 冻结记录已确认；LinkForty 同 Key 重复创建作为 V1.3.2 已知缺陷，不以 Redis 或本地通用表替代 |
| `DEC-M4-001~004`、`DEC-IMP-001/002`、`DEC-EXP-001/002`、`DEC-ANL-001` | 组织编码不可变；跨组织使用 Transfer；立即解绑；导入只校验；同步白名单导出；统计只读既有事件源且失败返回 503 | M4 Service/Router/Mock 测试已落地；M2 主数据、真实 PostgreSQL 并发、导出和统计实现测试继续推进 |

上表中的“本地已验证”表示代码、测试和契约已闭环；它不等价于 Casdoor、NFC、LinkForty API 生产配置、数据库运行权限和 Redis 延期项已关闭。只有所有外部门禁全部取得证据，冻结记录才可从 `PARTIAL_EXTERNAL_EVIDENCE_PENDING` 改为 `FROZEN`。

## 4. 外部证据关闭条件

外部证据不得用占位 URL、虚构 Claim、模拟负责人签字或 Mock 成功结果替代。每项证据必须包含：负责人角色、确认日期、环境、结论、脱敏样例或测试报告路径。

### Casdoor（延期至 V1.4）

- 必须提供测试 issuer、audience、JWKS URI 和允许算法。
- JWT 必须包含非空字符串 `employee_code` 和非空 `sub`。
- 后端默认使用 60 秒时钟偏差；员工不存在或停用返回 401/403。
- 未知 `kid` 触发 JWKS 刷新，不能保存私钥。

### LinkForty

- V1.3.2 的 `POST /api/links` 携带稳定 `Idempotency-Key`，但 Core 当前不执行服务端去重；同 Key 重复创建属于已知缺陷，银行侧不宣称 Exactly-once。
- 同 Key 同 Payload 返回首次对象；同 Key 不同 Payload 返回冲突。
- 4xx 不重试；429、5xx、超时使用指数退避，最多 3 次。
- V1.4 的只读账号不得读取 `webhooks` 或 `webhooks.secret`，不得执行 DML、DDL、TRUNCATE；V1.3.2 不建立或使用该账号，当前统计只调用 LinkForty API。

### NFC（延期至 V1.4）

- 确认 UID 读取、NDEF 编码、最大长度、写入失败和读回核验结果。
- 真机不是软件 MVP 的成功依据；Mock 必须能模拟失败且不得伪造真机成功。

## 5. 变更控制

- 冻结后的 API、字段、状态或权限变化必须创建新的 `DEC-*` 记录。
- 任何数据库修正必须新增 Alembic revision，不修改已应用 revision。
- 外部契约变化必须同时更新 Integration Adapter、OpenAPI、测试和本记录的证据状态。
- P0 只有在“外部证据待补”全部清零、验证命令有结果、负责人完成评审后才能改为 `FROZEN`。
