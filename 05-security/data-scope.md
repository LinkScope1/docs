# 数据范围方案

## 类型

- 本人：当前用户或客户经理关联的数据。
- 机构：授权组织及递归下级组织。
- 全局：全部组织数据。

## 规则

- 后端必须执行数据范围过滤。
- 前端传入的 `userId` 不能决定权限。
- 兼属组织不自动扩大权限。
- 公共目标资源对普通机构角色默认只读。
- 导出使用与页面查询相同的数据范围。
- 绑定、发布和停用命令必须同时校验功能权限和数据范围。
