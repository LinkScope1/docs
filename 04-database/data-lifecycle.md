# 数据生命周期

## 事件数据

- `access_events` 先按 MVP 要求不分区。
- 必须在上线前确认保留周期和归档方式。
- 归档不得破坏 `event_id` 去重和审计追溯。

## 审计数据

- `operation_logs` 只追加。
- 保留期由安全和合规负责人确认。
- 归档文件必须加密和限制访问。

## 测试数据

- 生产数据不得直接复制到开发环境。
- 生产样本必须脱敏。
- 测试数据必须可重复初始化。
